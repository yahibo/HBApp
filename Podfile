# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'HBApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HBApp
  
  #
#  pod 'HBBase', :git => 'git@github.com:yahibo/HBBase.git', :branch => 'main'
  
  # 基础模块
  pod 'HBBase',path: "Modules/HBBase"
  
  # 三方库
  pod 'Moya'

end
